# Validation — 8 September 2026

- `bun run test`: 31 files, **299 tests pass**.
- `bun run typecheck`: TypeScript and Effect diagnostics pass with zero errors, warnings or messages.
- `bun run lint` and `bun run format`: pass.
- `E2E_BUILT_WORKER=1 bunx playwright test e2e/live-sync.e2e.ts e2e/sync-auth.e2e.ts e2e/mcp.e2e.ts e2e/privacy.e2e.ts e2e/telemetry.e2e.ts`: **11 pass**.
- `bun run deploy:dry-run`: production build and Wrangler dry run pass. The existing large-chunk advisory remains.
- `bun run privacy:validate`: no errors; the existing undeclared-DPO warning remains.
- Client output contains none of the server exporter/token identifiers checked (`AXIOM_TOKEN`, `node:async_hooks`, `OtlpLogger`, `x-axiom-metrics`).

Focused regressions cover concurrent account/trace isolation through nested Promise callbacks, private annotation/cause removal, malformed optional sync metadata, streaming completion, unsampled Protobuf diagnostic/metric export, absence of account IDs from metrics, bounded export shutdown on 429/503, and queued performance-report deletion on consent withdrawal. Built-Worker tests cover browser admission limits and same-origin enforcement alongside actual authenticated DO/WebSocket, MCP OAuth, reload, offline replay, session revocation and privacy flows.

An earlier oversized-request test exposed an unread-body connection failure in the local Worker pipeline. Streaming the request through the byte limit and cancelling at the boundary fixes that regression. An intermittent collapse test passed on an untouched checkout, three consecutive isolated runs of this patch, and the final combined suite; no unrelated navigation implementation was changed.

At the initial validation, no production deployment, live Axiom ingestion/schema/query validation, notifier delivery test or production smoke test had been performed. See the subsequent deployment below. The owner subsequently reported manual setup complete, with code review pending. Platform export and independent uptime monitoring are omitted by request; platform and exporter failures are inspected manually in Cloudflare. The email notifier ID and rollout steps are in [the runbook](README.md) and [resource definitions](resources.json). Retention targets remain subject to account limits and have not been independently verified. The 48-hour overhead and seven-day threshold reviews are post-rollout tasks.

After removing the platform dataset, dependent panels/alerts and readiness endpoint, `bun run build`, `bun run format`, `bun run lint`, `bun run typecheck` and `E2E_BUILT_WORKER=1 bunx playwright test e2e/telemetry.e2e.ts` pass (one browser-ingestion test). The route tree was regenerated by the build. The full suites and deployment dry run listed above were run before this removal; they were not repeated for this follow-up.

## Readability cleanup and deployment

On 8 September 2026, separated exporter setup from invocation lifecycle handling, extracted browser admission/measurement and outbox observation steps, split MCP tool execution from response reporting, and replaced nested outcome/browser conditionals with explicit branches. Sampling, shutdown limits, consent and synchronization behavior remain covered by the existing regressions.

- `bun run test`: **299 tests pass** in 31 files.
- Built-Worker live-sync, sync-auth, MCP, privacy and telemetry E2E suite: **11 tests pass**.
- `bun run format`, `bun run lint`, `bun run typecheck`: pass; Effect diagnostics report zero errors, warnings and messages.
- `bun run deploy:dry-run`: production build and deployment dry run pass; the existing chunk-size advisory remains.
- `bunx wrangler deploy`: deployed Worker version `5fe8c33c-85df-47ae-aa6c-6145a1260b7c` to `sdqst.app` and `www.sdqst.app` with `TELEMETRY_ENABLED=true`. The production `AXIOM_TOKEN` secret is present.
- Production `/login`, `/privacy` and MCP discovery requests return **200** with telemetry correlation headers.

At deployment, Axiom monitor/dashboard creation and live query validation were deferred until application data arrived. The subsequent configuration and checks are recorded below. The corresponding authenticated board/MCP flows passed locally against the built Worker.

## Live Axiom configuration and validation

On 8 September 2026, inspected the ingested log/trace schemas, metric descriptors and tags. The trace dataset is recognized as `otel.traces` with native `trace_id`, `span_id`, `parent_span_id` and duration fields. Verified a 24-span browser → WebSocket → Durable Object → D1 trace, `fa40d38086e941fe8e3248e605da34c3`, at `2026-09-08T21:26:15.506Z`. This confirms a production cross-boundary flow; it is not a claim that every webhook/MCP production scenario was exercised.

Created five dashboards with 40 data panels and four shared filters. Created three enabled email monitors covering the eight planned conditions within the Personal plan's three-monitor limit. All **47 unique queries pass against Axiom**: 40 panel queries, four filter queries and three monitor queries. Re-exported every dashboard and confirmed all stored APL/MPL query strings match the repository definitions. Resource identifiers are in [resources.json](resources.json); machine-readable results are in [validation.json](validation.json).

Threshold checks used query-only fixtures, leaving production datasets unchanged. Positive cases exercise persistence, migrations, MCP recovery, verified billing, HTTP/RPC failures, integration failures, interactive latency, acknowledgement timeouts, authorized stuck edits and browser regressions. Negative cases verify expected rejections, missing billing verification, insufficient failures/sessions, unauthorized/offline outboxes, exact 5% and 10% rates, and exact two-second latency. Production queries returned no breached conditions during validation.

Temporarily added a labelled, automatically expiring `notification_test` branch to the critical monitor. Axiom recorded **trigger at 21:36:55.738 UTC** and **resolution at 21:37:53.028 UTC**. The original production query was restored. The existing notifier points to `x@jxd.dev`; mailbox receipt and email-provider delivery status are not exposed by the connector. Check the inbox/spam folder for the Sidequest critical data and billing test alert and recovery around **22:37–22:38 London time**. No application failure was injected.

`bun run format`, `bun run lint`, `bun run typecheck` and `git diff --check` pass after this configuration work. No application code changed in this step, so application tests, build and deployment were not repeated.

Verification limits:

- Dashboard rendering and interactive filter binding were not inspected in an authenticated browser. The query tool does not accept dashboard parameters for MPL; those five queries were validated after expanding the documented filter clauses to the default values (production, other filters unset). Dashboard definitions retain the documented `$filter`/`ifdef` syntax.
- The connector exposes no organization URL, standalone saved-query creation, or exact 30-minute notification repeat option. The inventory records IDs; dashboards retain their queries. Trigger/recovery notifications are configured with per-minute repeats disabled.
- Axiom dashboard notes and monitor descriptions include immediate triage steps and the repository runbook path. They do not depend on publishing the current working-tree documentation. No commit was created.
- Dataset/token/retention setup was reported complete by the owner; retention settings are not exposed by the connected tools. The 48-hour overhead and seven-day threshold reviews remain scheduled follow-up work, not completed checks.
